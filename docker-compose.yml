services:
  web:
    build: .
    container_name: django_app
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DEBUG=True
    command: uv run python manage.py runserver 0.0.0.0:8000
# section volumes: supprimée